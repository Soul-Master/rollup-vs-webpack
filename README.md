# rollup-webpack-compare

Just open console and run the following command. It should create bundles to `build` folder.

```
$ npm start
```

PS. This project was created by `Polymer-cli` for `Polymer 2.0`.